<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@drawable/calmscape_logo"
        android:label="@string/app_name"
        android:roundIcon="@drawable/calmscape_logo"
        android:supportsRtl="true"
        android:theme="@style/Theme.CalmScape"
        tools:targetApi="31">
        <activity
            android:name=".activities.FinalActivity"
            android:exported="false" />
        <activity
            android:name=".activities.DiaryActivity"
            android:exported="false" />
        <activity
            android:name=".activities.CalendarActivity"
            android:exported="false" />
        <activity
            android:name=".activities.Task_Checklist"
            android:exported="false" />
        <activity
            android:name=".activities.EmojiActivity"
            android:exported="false" />
        <activity
            android:name=".activities.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>